This is a helper repository to easily build the Shim used by 
toolhouse DV-Systeme GmbH & Co. KG for Secure Boot.

To build the shim copy the public key of a certificate to this directory 
and update the CERT environment variable in build_shim.

Then call ./build_shim to run the build process
